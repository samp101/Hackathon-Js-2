let blogTitle = document.querySelector(
  "#blog-title"
);
let blogAuthor = document.querySelector(
  "#blog-author"
);
let blogCatgory = document.querySelector(
  "#blog-category"
);
let blogPost =
  document.querySelector("#blog-post");
let form = document.querySelector("form");
console.log(
  new Date().toLocaleDateString("fr-CA")
);

function createPost(e) {
  let date = new Date().toLocaleDateString(
    "fr-CA"
  );

  const randomNum =
    Math.floor(Math.random() * 100) + 1;
  blogTitleVal = blogTitle.value;
  blogAuthorVal = blogAuthor.value;
  blogCatgoryVal = blogCatgory.value;
  blogPostVal = blogPost.value;

  let newPost = {
    user_id: 1,
    title: blogTitleVal,
    category: blogCatgoryVal,
    post: blogPostVal,
    img_url:
      "https://picsum.photos/id/" +
      randomNum +
      "/300",
    date_created: date,
  };

  fetch("/uploadpost", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
    },
    body: JSON.stringify(newPost),
  })
    .then((res) => res)
    .catch((err) => console.log(err));
}
